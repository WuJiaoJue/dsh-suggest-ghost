# dsh-suggest-ghost bundle patch：把 host 插件行插入 loader 树。
# 作为 dsh.bundle.patch 自动挂载（dsh plugin add 后即生效）；
# client 端通过 package.json 的 dsh.client 声明进入 boot 图。
- insert:
  - id: suggest-ghost
    name: dsh-suggest-ghost
